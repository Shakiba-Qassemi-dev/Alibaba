/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['example.com'], // دامنه‌های عکس‌های خارجی
    },
};

export default nextConfig;
